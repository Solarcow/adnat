<% provide(:title, @user.name) %>
<div class="cointer">
  <aside class="col-md-4">
    <section class="user_info">
        <p> Logged in as user <%= @user.name %>   <%= link_to "Log out", logout_path, method: :delete %></p>

    </section>
  </aside>
</div>